var Default = ht.Default,
    def = Default.def,
    ui = ht.ui,
    NULL = null;

Default.setImage('rate_star', {
    width: 100,
    height: 100,
    comps: [
        {
            type: 'star',
            rect: [0, 0, 100, 100],
            background: 'rgb(255,255,255)'
        }
    ]
});